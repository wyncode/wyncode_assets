require 'spec_helper'

describe WyncodeAssets do
  it 'has a version number' do
    expect(WyncodeAssets::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
